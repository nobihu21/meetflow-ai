{
  "name": "meetflow-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:client dev:server",
    "dev:client": "pnpm --filter @meetflow/client run dev",
    "dev:server": "pnpm --filter @meetflow/server run dev",
    "build": "pnpm --filter @meetflow/client run build && pnpm --filter @meetflow/server run build",
    "typecheck": "pnpm --filter @meetflow/client run typecheck && pnpm --filter @meetflow/server run typecheck"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.5.4"
  }
}
